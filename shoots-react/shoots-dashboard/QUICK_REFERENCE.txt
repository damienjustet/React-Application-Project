╔════════════════════════════════════════════════════════════════╗
║           CURRENCY INPUT - QUICK REFERENCE CARD                ║
╚════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│ HOW TO USE (For Users)                                         │
└────────────────────────────────────────────────────────────────┘

  Rule: Type the amount without a decimal point
        Last 2 digits = cents

  Examples:
    Want $8.00?    → Type: 800
    Want $12.99?   → Type: 1299
    Want $0.50?    → Type: 50
    Want $100.00?  → Type: 10000

┌────────────────────────────────────────────────────────────────┐
│ HOW TO IMPLEMENT (For Developers)                              │
└────────────────────────────────────────────────────────────────┘

  1. Import the component:
     import CurrencyInput from './components/CurrencyInput'

  2. Use in your form:
     <CurrencyInput
       value={amount}
       onChange={(dollars) => setAmount(dollars)}
       placeholder="0.00"
     />

  3. State should be a number (not string):
     const [amount, setAmount] = useState(0)

┌────────────────────────────────────────────────────────────────┐
│ COMMON PATTERNS                                                 │
└────────────────────────────────────────────────────────────────┘

  Input     →  Display  →  Stored
  ──────────────────────────────────
  5         →  0.05     →  0.05
  50        →  0.50     →  0.50
  500       →  5.00     →  5.00
  1299      →  12.99    →  12.99
  10000     →  100.00   →  100.00

┌────────────────────────────────────────────────────────────────┐
│ FILES IN THIS SYSTEM                                            │
└────────────────────────────────────────────────────────────────┘

  📁 src/utils/currencyUtils.js
     └─ Core conversion and formatting functions

  📁 src/components/CurrencyInput.jsx
     └─ Reusable React component

  📁 src/pages/SpendingPage.jsx
     └─ Implementation example (Add & Edit modals)

┌────────────────────────────────────────────────────────────────┐
│ KEY FEATURES                                                    │
└────────────────────────────────────────────────────────────────┘

  ✓ No decimal point needed
  ✓ Mobile-friendly (numeric keyboard)
  ✓ Auto-formatting on blur
  ✓ Raw value on focus (for editing)
  ✓ Validates input (numbers only)
  ✓ Max $999,999.99
  ✓ Works on all devices

┌────────────────────────────────────────────────────────────────┐
│ VALIDATION                                                      │
└────────────────────────────────────────────────────────────────┘

  Check if amount is valid:
    if (amount > 0) {
      // Valid transaction
    }

  Amount is stored as a number, ready to use!

┌────────────────────────────────────────────────────────────────┐
│ UTILITY FUNCTIONS AVAILABLE                                     │
└────────────────────────────────────────────────────────────────┘

  import {
    inputToCents,           // "1299" → 1299
    formatCentsAsDollars,   // 1299 → "12.99"
    formatDollarDisplay,    // 12.99 → "$12.99"
    getInputAsDollars,      // "1299" → 12.99
  } from './utils/currencyUtils'

╔════════════════════════════════════════════════════════════════╗
║  Test the live implementation at: http://localhost:5174/       ║
║  Click "Add Transaction" to try the new currency input!        ║
╚════════════════════════════════════════════════════════════════╝
